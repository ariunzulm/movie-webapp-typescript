import { ThemeProvider } from "../homescreen/_components/theme-provider";

type RootLayoutProps = {
  children: React.ReactNode;
};

export default function SearchLayout({ children }: RootLayoutProps) {
  return (
    <html lang="en" suppressHydrationWarning>
      <head />
      <body>
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          <main>{children}</main>
        </ThemeProvider>
      </body>
    </html>
  );
}
